query GetPeoplePageDisplayNamePerLanguage($itemId: String!) {
  item(path: $itemId, language: "en") {
    id
    path
    languages {
      language {
        name
      }
      displayName
    }
  }
}
